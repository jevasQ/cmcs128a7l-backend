'use strict'
const db = require('../db/db');

exports.addCur = (req, res) => {
	/*var id = req.body.id;
	var name = req.body.name;
	
	db.connect();
	
	db.query('INSERT INTO student VALUES("2017-12345", "John Doe","1998-08-20");', function(error, results, fields){
		if(error) throw error;
		//do everything inside this
		res.send(results);
		res.end('message');
	});*/
}
